# Generated report output (the `report`/`map` commands write here)
.ai-usage/

# Scratch design mockups — already ported into src/templates/*; kept on disk
# for reference but not tracked.
mockup-report.html
mockup-report-v2.html

# OS / editor noise
.DS_Store
